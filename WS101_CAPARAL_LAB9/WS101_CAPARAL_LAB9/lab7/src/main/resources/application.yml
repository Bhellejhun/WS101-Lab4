spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/testdb
    username: root
    password: CCS_L1

  jpa:
    hibernate:
      ddl-auto: update   # create or update schema automatically
    show-sql: true
    properties:
      hibernate:
        format_sql: true
